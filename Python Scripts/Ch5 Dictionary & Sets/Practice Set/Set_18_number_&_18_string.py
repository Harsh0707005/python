# Can we have a set with 18(int) and 18(string)?

a = {18, "18"}
print(a)

'''
Hence, Yes we can have a set with 18 as a integer and 18 as a string
as python interprets both the elements differently, One as a string and the other as an integer.
'''